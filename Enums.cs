namespace LudoT_Simulation;

public enum Color
{
    Red,
    Yellow,
    Green,
    Blue
}

public enum Direction
{
    Clockwise,
    CounterClockwise
}

public enum PieceState
{
    Base,
    StandardPath,
    HomeStraight,
    Home
}

public enum StatusEffect
{
    Normal,
    Energized,
    Sick,
    Briefing
}
